(function () {

    // Prevent variables from leaking into the parent scope.
    'use strict';

    // Define the overall menu app module.
    angular.module('MenuApp', ['ui.router', 'data']);

})(window);
